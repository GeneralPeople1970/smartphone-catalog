; Application PHP limits. Must be >= the app's own upload limits so the app
; validation (not PHP) is the final, friendly gatekeeper. Carousel uploads are
; capped at 20M by the app; allow headroom for multipart overhead.
upload_max_filesize=24M
post_max_size=24M
memory_limit=256M
max_execution_time=60
expose_php=Off
